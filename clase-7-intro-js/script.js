//TIPO de variables var,let,const
//TIPO DE DATOS number,string--> '',"",``,boolean, arrays(o listas)[miebro1,miebro2....],objetos{propiedad:valor,propiedad2:valor2,....},null,undefined 
//OPERADORES ---> Aritmeticos ( +-....), asignacion (= +=....) , comparacion( ==, === ,!=, !== <>-....) ---> BOOLEANO (TRUE o FALSE), logicos (&&,|| ,! ) ---> BOOLEANO  (TRUE o FALSE)
// CONTROLES DE FLUJO 
    // CONDICIONALES  if, else if, else  || ? -->  {condicion}? {bloque que se ejecuta si la condicion es true}: {bloque que se ejecuta si no se cumple la condicion}
    // BUCLES : for(let i = 0;condicion; incremento de la variable)
    //          for in --> PARA OBJETOS .... for({variable donde asignamos la propiedad de la iteracion } in {variable de tipo objeto})
    //          for of --> PARA LISTAS O ARRAYS for ({variable donde asignamos miembro del array} of {variable del array que recorremos})
    
    //          while--> condicion while(condicion){bloque de codigo que se ejecuta mientras la condicion se cumpla}


    //FUNCIONES , funciones clasicas  y funciones flecha
    // ARRAY y objetos.....
    // COPIAS DE OBJETOS Y ARRAYS
